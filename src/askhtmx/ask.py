from funcchain import chain


def ask_htmx(customer_question: str) -> str:
    """
    Answer the question about htmx. If the question is not about htmx, then
    tell the customer that we only answer questions about htmx.
    """
    return chain()
